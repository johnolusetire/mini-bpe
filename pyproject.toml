[project]
name = "mini-bpe"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "regex>=2024.11.6",
    "tiktoken>=0.9.0",
    "wget>=3.2",
]
